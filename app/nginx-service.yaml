apiVersion: v1
kind: Service
metadata:
  labels:
    app: nginx-demo
  name: nginx-demo
  namespace: default
spec:
  ports:
    - port: 9080
      protocol: TCP
      targetPort: 80
  selector:
    app: nginx-demo
  type: LoadBalancer
